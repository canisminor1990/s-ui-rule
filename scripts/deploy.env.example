# Copy to .deploy/config (gitignored) or export as environment variables.
# GitHub Actions uses repository Secrets instead — do not put real values here.
SUI_HOST=
SUI_USER=
# Optional. If unset, ssh uses the default agent / ~/.ssh config.
# SUI_SSH_KEY=
